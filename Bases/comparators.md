On peut comparer deux variable avec ces éléments :
```
var1 && var2 // une deux deux var doivent etre vrai
var1 || var2 // les deux var doivent etre vrai 
```

pour vérifier on peux mettre ces opérateurs
`==
ou
`===


(mettre les falsy value et les truely value)

comme racourci existant il existe aussi ceux-ci :
```
i-- // enleve un
i++ // rajoute un
i+=10 // rajoute 10
i-+10 // enleve 10
i*=10 // multiplie par 10
i/=10 // divise par 10
```